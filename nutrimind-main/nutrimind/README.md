# 🥗 NutriMind Flutter App — Firebase Edition

**Your Personal Editorial Food Sanctuary** for Davao, Philippines.
Full-stack Flutter app with Firebase Authentication, Firestore database, and fully functional UI.

---

## 🔥 Firebase Setup (Required Before Running)

### Step 1 — Create Firebase Project
1. Go to [console.firebase.google.com](https://console.firebase.google.com)
2. Click **Add project** → Name it `nutrimind`
3. Enable **Google Analytics** (optional)

### Step 2 — Enable Authentication
1. Firebase Console → **Authentication** → **Sign-in method**
2. Enable **Email/Password**
3. Enable **Google** (add your SHA-1 key for Android)

### Step 3 — Create Firestore Database
1. Firebase Console → **Firestore Database** → **Create database**
2. Start in **test mode** (change rules before production)
3. Choose your region (e.g. `asia-southeast1` for Philippines)

### Step 4 — Firestore Security Rules
Go to **Firestore → Rules** and paste:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /users/{userId}/meals/{mealId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /posts/{postId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null;
      allow delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }
    match /posts/{postId}/comments/{commentId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
  }
}
```

### Step 5 — Connect Flutter to Firebase

Install the FlutterFire CLI:
```bash
dart pub global activate flutterfire_cli
```

Run in your project root:
```bash
flutterfire configure
```
Select your Firebase project. This auto-generates `lib/firebase_options.dart` with correct values.

### Step 6 — Android Setup
In `android/app/build.gradle`, make sure:
```gradle
defaultConfig {
    minSdkVersion 21
    ...
}
```

For Google Sign-In on Android, add your SHA-1 key:
```bash
cd android && ./gradlew signingReport
```
Copy the **SHA-1** from `debug` variant → paste in Firebase Console → Project Settings → Your Android app.

### Step 7 — iOS Setup (if needed)
In `ios/Runner/Info.plist`, add the `REVERSED_CLIENT_ID` from your `GoogleService-Info.plist`:
```xml
<key>CFBundleURLTypes</key>
<array>
  <dict>
    <key>CFBundleURLSchemes</key>
    <array>
      <string>YOUR_REVERSED_CLIENT_ID</string>
    </array>
  </dict>
</array>
```

---

## 🚀 Running the App

```bash
flutter pub get
flutter run
```

---

## 📁 Project Structure

```
lib/
+-- main.dart                    # App entry + Firebase init + Provider setup
+-- firebase_options.dart        # Generated by flutterfire configure
+-- theme/
|   +-- app_theme.dart           # Legacy design system colors
|   +-- modern_app_theme.dart    # Active global theme (ModernAppTheme)
+-- models/
|   +-- user_model.dart          # User data model
|   +-- meal_model.dart          # Meal data model + enums
|   +-- post_model.dart          # Post, Comment, Report models
|   +-- notification_model.dart  # In-app notification model
|   +-- badge_model.dart         # Badge model
|   +-- weekly_stats_model.dart  # Engagement stats model
|   +-- pantry_item_model.dart   # Pantry item model
|   +-- scanned_item_model.dart  # Scanner history model
|   +-- recipe_model.dart        # Recipe model
+-- services/
|   +-- auth_service.dart        # Firebase Auth (Email + Google)
|   +-- firestore_service.dart   # All Firestore CRUD operations
|   +-- engagement_service.dart  # Badges, weekly stats, leaderboard
|   +-- meal_planner_service.dart # DSS/knapsack optimizer
|   +-- nutribot_service.dart    # Groq chat integration
|   +-- food_image_resolver.dart # Local food asset mapping
+-- providers/
|   +-- auth_provider.dart       # Auth state + user data
|   +-- meal_provider.dart       # Meal plan state
|   +-- community_provider.dart  # Posts & comments state
|   +-- notification_provider.dart # Notifications state
+-- utils/
|   +-- firestore_safety.dart    # safeDouble(double? v)
+-- config/
|   +-- engagement_config.dart   # Badge/point rules
|   +-- community_config.dart    # Categories/tags/report reasons
+-- widgets/
|   +-- safe_image.dart          # SafeFoodImage, SafeAvatar
|   +-- nutribot/                # NutribotLauncher, NutribotPanel
+-- screens/
    +-- auth/
    |   +-- auth_gate.dart       # Auth router
    |   +-- login_screen.dart    # Sign in
    |   +-- register_screen.dart # Sign up
    +-- onboarding/
    |   +-- splash_screen.dart   # Landing page
    |   +-- goal_screen.dart     # Goal selection
    |   +-- biometrics_screen.dart # Height/weight/age
    +-- main/
        +-- main_shell.dart       # Bottom nav + FAB + NutriBot
        +-- home_screen.dart      # Dashboard
        +-- meal_plan_screen.dart  # Weekly meal log
        +-- community_screen.dart  # Social feed
        +-- profile_screen.dart    # Profile + settings
        +-- ai_meal_planner_screen.dart # DSS optimizer
        +-- recipe_browser_screen.dart  # Recipe browsing
        +-- food_scanner_screen.dart    # AI food scanner
```

---

## 🗄️ Firestore Data Structure

```
users/{uid}
  name, email, photoUrl, location
  goal, gender, height, weight, age
  dailyBudget, allowNonLocal, budgetBuffer, budgetConfigured
  isPremium, profileCompleted, createdAt

users/{uid}/meals/{mealId}
  name, type, price, calories
  status (planned/logged)
  date, loggedAt
  protein, carbs, fat, ingredients
  recipe, cookingSteps, imageUrl

users/{uid}/notifications/{id}
  category, type, title, message, isRead, createdAt

users/{uid}/pantry_items/{itemId}
  name, quantity, unit, category, source

users/{uid}/scanned_items/{scanId}
  name, calories, price, mealType, source

users/{uid}/weeklyStats/{weekId}
  points, mealLogged, scannedMeal, etc.

users/{uid}/badges/{badgeId}
  title, description, earnedAt

users/{uid}/followers/{uid}
users/{uid}/following/{uid}

users/{uid}/weekly_palengke_lists/{weekId}/items/{itemId}

posts/{postId}
  userId, userName, userPhotoUrl
  content, category, tags
  likeCount, commentCount
  imageUrl, createdAt
  moderationStatus, reportCount

posts/{postId}/comments/{commentId}
  userId, userName, content, createdAt

posts/{postId}/likes/{uid}
  uid, createdAt

posts/{postId}/reports/{reporterId}
```

---

## ✅ Functional Features

| Feature | Status |
|---------|--------|
| Email/Password Sign Up | ✅ |
| Google Sign In | ✅ |
| Forgot Password (email reset) | ✅ |
| Onboarding (Goal + Biometrics) | ✅ |
| Home Dashboard with real data | ✅ |
| Daily budget tracker | ✅ |
| Log meals (quick tap) | ✅ |
| Add meals manually | ✅ |
| Weekly meal plan calendar | ✅ |
| Generate AI meal plan (local DSS) | ✅ |
| Delete meals (swipe) | ✅ |
| Community feed (real-time) | ✅ |
| Like / unlike posts | ✅ |
| Create posts with categories + tags | ✅ |
| Comments (real-time stream) | ✅ |
| Delete own posts | ✅ |
| Profile with live Firestore data | ✅ |
| DSS Settings (budget, buffer, local toggle) | ✅ |
| Edit profile (save to Firestore) | ✅ |
| Sign out with confirmation | ✅ |
| Loading states on all async actions | ✅ |
| Error handling with SnackBars | ✅ |
| Form validation | ✅ |

---

## 🎨 Design System

| Token | Value |
|-------|-------|
| Primary Green | `#2D6A4F` |
| Light Green | `#52B788` |
| Accent Green | `#74C69D` |
| Soft Green | `#D8F3DC` |
| Dark Green | `#1B4332` |
| Orange Accent | `#FF8C42` |
| Error Red | `#E53E3E` |
